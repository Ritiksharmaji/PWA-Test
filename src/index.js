import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App.js';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<App />);



// This code initializes a React application by importing the necessary modules and rendering the main App component into the root element of the HTML document.